mutation linkService($input: LinkServiceInput!) {
  linkService(input: $input) {
	serviceLink {
	  id,
	  createdAt,
	  updatedAt,
	  type,
	  accessToken,
	  refreshToken,
	  expiresIn,
	  service {
		id
	  }
	}
  }
}
